version: 2

models:
  - name: stg_events
    columns:
      - name: event_id
        data_tests: [not_null, unique]
  - name: fct_daily_active_users
    columns:
      - name: event_date
        data_tests: [not_null, unique]
